export enum DeliveryMethodEnum {
  INSTANT = "instant",
  STANDARD = "standard",
}

export enum LevelCook {
  EASY = "easy",
  MEDIUM = "medium",
  HARD = "hard",
}

export enum Role {
  ADMIN = "ADMIN",
  CUSTOMER = "CUSTOMER",
  SHIPPER = "SHIPPER",
}

export enum SortEnum {
  NEWEST = "newest",
  POPULAR = "popular",
  PRICE = "price",
}

export enum OrderByEnum {
  ASC = "ASC",
  DESC = "DESC",
}
